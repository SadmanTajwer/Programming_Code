#include <stdio.h>
#include<math.h>
int main ()
{
    int n,i,population,average;
    printf("Enter any number you want to know :\n");
    scanf("%d",&n);
    for (i=1;i<n;i++)
    {
        scanf("%d",&population);
        printf("%d\n",population);
        {
        (average=population/n);}
        printf("average people of those countries:%d",average);
    }
}
